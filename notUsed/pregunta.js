const querystring = require('querystring');
const https = require("https");

/**
 * @type {import("../bot").Command}
 */
module.exports = {
  usage: "[peli o serie]",
  description: "Busca el nombre de una pelicula o serie en una base de datos y devuelve una tarjeta de información",
  exec: ({split, message}) => {

  }
}